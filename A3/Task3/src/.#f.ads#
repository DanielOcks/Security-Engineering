function F(N: Natural) return Natural is
    I: Natural := 0;
    X: Natural := 1;
begin --def(N)
    while I < N loop --def(I), p-use(I), c-use(N)
        I := I + 1;  --def(I), c-use(I)
        X := X * I; --def(X), c-use(X, I)
    end loop;

    return X; -- c-use(X)
end F;
